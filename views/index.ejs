<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Outlook Email Assistant</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <div class="container mt-4">
    <h1 class="mb-4">Outlook Email Assistant</h1>
    
    <% if (!isAuthenticated) { %>
      <div class="card">
        <div class="card-body">
          <h5 class="card-title">Welcome to Outlook Email Assistant</h5>
          <p class="card-text">This application helps you manage your Outlook emails by:</p>
          <ul>
            <li>Reading unread emails</li>
            <li>Checking your calendar availability</li>
            <li>Generating draft responses</li>
          </ul>
          <p>Please sign in with your Microsoft account to continue.</p>
          <a href="/auth/login" class="btn btn-primary">Sign in with Microsoft</a>
        </div>
      </div>
    <% } else { %>
      <div class="card">
        <div class="card-body">
          <h5 class="card-title">Welcome, <%= userName %></h5>
          <p class="card-text">You are signed in. What would you like to do?</p>
          <a href="/emails" class="btn btn-primary">View Unread Emails</a>
          <a href="/status" class="btn btn-outline-info">Check System Status</a>
          <a href="/auth/logout" class="btn btn-outline-secondary">Sign Out</a>
        </div>
      </div>
    <% } %>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  <script src="/js/main.js"></script>
</body>
</html>